export type Product = {
  id: string;
  slug: string;
  name: string;
  description: string;
  price: number;
  unit: string;
  stock: number;
  categoryId: string;
  producerId: string;
  producerName: string;
  imageUrl: string;
};
